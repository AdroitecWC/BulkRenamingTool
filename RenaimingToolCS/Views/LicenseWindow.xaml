<Window x:Class="RenaimingToolCS.Views.LicenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RenaimingToolCS.Views"
        mc:Ignorable="d"
        Title="LicenseWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- Company Theme Resources -->
        <SolidColorBrush x:Key="BackgroundColor" Color="White"/>
        <SolidColorBrush x:Key="ForegroundColor" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="ButtonBackground" Color="#2E3A59"/>
        <SolidColorBrush x:Key="ButtonForeground" Color="White"/>
        <SolidColorBrush x:Key="DataGridHeaderBackground" Color="#2E3A59"/>
        <SolidColorBrush x:Key="DataGridHeaderForeground" Color="White"/>

        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                        CornerRadius="20"
                        Background="{TemplateBinding Background}"
                        Padding="11,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#17A2B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>




        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource DataGridHeaderBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource DataGridHeaderForeground}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="70"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>



        <!-- Windchill-style Filter Label -->
        <Style x:Key="FilterLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2E3A59"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <!-- Windchill-style ComboBox -->
        <Style x:Key="WindchillComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2E3A59"/>
            <Setter Property="BorderBrush" Value="#2E3A59"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="#2E3A59"/>
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Padding" Value="4,2"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="Normal"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="#2E3A59"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="MainBorder"
                        CornerRadius="12"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ToggleButton Name="ToggleButton"
                                      
                                      Grid.Column="2"
                                      Focusable="false"
                                      IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      ClickMode="Press"
                                      Background="Transparent"
                                      BorderThickness="0"/>
                                <ContentPresenter Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"
                                          Margin="6,0"/>
                                <Popup Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                                    <Border Background="White"
                                    BorderBrush="#DDD"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                        <ScrollViewer Margin="0"
                                              SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox ToggleButton (arrow button) -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border x:Name="Border"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            Background="{TemplateBinding Background}">
                <Grid>
                    <Path x:Name="Arrow"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M 0 0 L 4 4 L 8 0 Z"
                  Fill="#666"/>
                </Grid>
            </Border>
        </ControlTemplate>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2E3A59"/>
            <Setter Property="Padding" Value="4,2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E6E6E6"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="#2E3A59"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <!-- Windchill-style TabControl -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#C0C0C0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <!-- Windchill-style TabItem -->
        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2,0,2,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2E3A59"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="0,0,0,2"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#2E3A59"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="0,0,0,3"/>
                                <Setter Property="Foreground" Value="#2E3A59"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#2E3A59"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <!-- Header with Adroitec Logo -->
        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="License Activation"
               FontSize="28" FontWeight="Bold"
               Foreground="{DynamicResource ForegroundColor}"
               VerticalAlignment="Center"/>

            <Image Source="pack://application:,,,/Resources/logo.png"
           Height="50"
           Grid.Column="1"
           Margin="10,0,0,0"
           VerticalAlignment="Center"
           Stretch="Uniform"/>
        </Grid>


        <!-- Form Fields -->
        <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Stretch">

            <StackPanel Margin="0,0,0,5">

                <DockPanel LastChildFill="True">
                    <Button x:Name="btnBrowse"
            Content="Browse License File"
            Style="{StaticResource RoundedButtonStyle}"
            Margin="0,0,10,0"
            MinWidth="140"
            HorizontalAlignment="Left"
            Click="btnBrowse_Click"/>

                    <Border CornerRadius="8" Background="White" BorderBrush="#2E3A59" BorderThickness="1" Height="32" 
            DockPanel.Dock="Left">
                        <TextBox x:Name="txtFilePath"
                 Background="Transparent" BorderThickness="0"
                 VerticalContentAlignment="Center" Padding="8,0"/>
                    </Border>
                </DockPanel>
            </StackPanel>
            <!-- System Name -->
            <StackPanel Margin="0,0,0,5">
                <TextBlock Text="System Name"
                           Style="{StaticResource FilterLabelStyle}" Margin="5"/>
                <Border CornerRadius="8" Background="White" BorderBrush="#2E3A59" BorderThickness="1" Height="32">
                    <TextBox x:Name="txtSystemName" IsReadOnly="True"
                             Background="Transparent" BorderThickness="0"
                             VerticalContentAlignment="Center" Padding="8,0"/>
                </Border>
            </StackPanel>

            <!-- Request Code -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Request Code"
                           Style="{StaticResource FilterLabelStyle}" Margin="5"/>
                <Border CornerRadius="8" Background="White" BorderBrush="#2E3A59" BorderThickness="1" Height="32">
                    <TextBox x:Name="txtRequestCode" IsReadOnly="True"
                             TextWrapping="Wrap"
                             Background="Transparent" BorderThickness="0"
                             VerticalContentAlignment="Center" Padding="8,0"/>
                </Border>
            </StackPanel>

            <!-- License File Path -->

            <!-- Network License Server -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Network License Server"
                           Style="{StaticResource FilterLabelStyle}" Margin="5"/>
                <DockPanel LastChildFill="True">
                    <Button x:Name="btnConnectServer"
                            Content="Connect"
                            Style="{StaticResource RoundedButtonStyle}"
                            Margin="10,0,0,0"
                            MinWidth="100"
                            DockPanel.Dock="Right"
                            Click="btnConnectServer_Click"/>
                    <Border CornerRadius="8" Background="White" BorderBrush="#2E3A59" BorderThickness="1" Height="32">
                        <TextBox x:Name="txtNetworkServer"
                                 Background="Transparent" BorderThickness="0"
                                 VerticalContentAlignment="Center" Padding="8,0"/>
                    </Border>
                </DockPanel>
                <TextBlock Text="e.g. vizserver:1122 — the name and port shown on the FloatingLicenseServer app"
                           FontSize="11" Foreground="Gray" Margin="5,4,0,0"/>
            </StackPanel>

            <!-- Expiry Label -->
            <TextBlock x:Name="lblExpiry"
                       FontWeight="Bold"
                       Foreground="Green"
                       Visibility="Collapsed"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,0"
                       Text="License expires on..." />

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
                <Button x:Name="btnActivate"
                        Content="Activate"
                        Width="120"
                        Style="{StaticResource RoundedButtonStyle}"
                        Click="btnActivate_Click"/>
                <Button Content="Cancel"
                        Width="120"
                        Style="{StaticResource RoundedButtonStyle}"
                        Click="btnCancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
